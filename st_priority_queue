#include <bits/stdc++.h>
using namespace std;
bool compare(int x, int y)
{
    return x>y;
}
int main() {
    priority_queue<int> pq;
    int a[10];
    for(int i=0;i<10;i++)
    {
        cin>>a[i];
        pq.push(a[i]);
    }
    while(!pq.empty())
    {
        cout<<pq.top()<<" ";
        pq.pop();
    }
    return 0;
}
