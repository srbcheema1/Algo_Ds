#include <iostream>
#include<cstring>
using namespace std;
int main() {
    char str[]=" HELLO MYSELF PRAJJWAL SINGH, AND A GOOD BOY";
    char *ptr;
    ptr=strtok(str," ");
    while(ptr!=NULL)
    {
        cout<<ptr<<endl;
        ptr=strtok(NULL,", ");

    }
    return 0;
}
